# Reopen in devcontainer
This is meant to be run in the devcontainer provided
# Terraform
```
cd terraform
terraform init
terraform plan
terraform apply
```
# Local
create .env file<br>
add is_localstack=true